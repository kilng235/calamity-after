# 灾厄之后·重制版 - 项目状态

> 更新时间：2026-09-06
> 项目阶段：ALPHA——引擎/记忆/身份三大地基完成（代码就绪、stub 验证通过），记忆系统 P1 账本套件已落地；进入真实长跑游玩验证期

---

## 🧭 系统全景（截至 2026-09-06）

| 层 | 系统 | 状态 |
|---|---|---|
| 数据与提示词 | yaml 转换器 / 世界书触发引擎（117 条目）/ 骰子池实时展开 / ST 预设导入器+宏引擎+管理面板 / 采样参数（温度·输出·上下文预算·惩罚） | ✅ 完成态 |
| 机制引擎 | 命令引擎（14 可写根+护栏）/ 检定（d20·优势·对抗·DC 五档）/ 战斗（耐久·双持·状态·先攻）/ 装备锻造炼金词缀 / 法术 / 状态 / 经验成长 / 任务块协议 / 关系（好感·人情·势力声望） | ✅ 可玩 |
| **记忆系统 2.0** | 五层架构 + 副 API + 撤销四层联动 | ✅ 代码完成，🟡 实玩未验证 |
| **记忆系统 P1·账本套件** | 实体台账（[实体更新] 块）/ 悬念簿（[悬念]/[悬念核销]）/ 主观记忆批量提取（挂纪事压缩·走副API·主演/龙套分级注入）/ 记忆库 6 页签 | ✅ 代码完成（38 项测试通过），🟡 实玩未验证 |
| **身份体系** | 开局包 / 领域优势+叙事视角 / 命运点联动 | ✅ 一二档完成，🟡 第三档（专属支线+声望）未做 |
| UI/体验 | 六维面板 / 任务简报 / 回合撤销 / **回合终止** / 物品卡片 / 时间位置 / 记忆库查看器 / 预设面板 | ✅ 完成 |
| 地图内容 | 10 区域 + 锈钉镇 4 地点 + 5 势力据点 + 17 NPC（世界书条目齐备，叙事可达，无图形地图） | ✅ 内容齐备 |

---

## ✅ 已完成工作

### 2026-09-06 批：记忆系统 P1 账本套件

#### 1. 数据层（memory-store.js v1→v2）
- ✅ `calamity-memory` 新增三个 store：`ledger`（实体台账）/ `suspense`（悬念簿）/ `subjective`（主观记忆）；onupgradeneeded 补建，旧 v1 数据无损

#### 2. 实体台账（蓝图步骤 10）
- ✅ `[实体更新]` 块协议（`<content>` 外，与任务块同区域；一个块多实体、空行分隔；名称/类别/别名/状态/变更）
- ✅ upsert + 别名双向归并（规范名保持首次登记名，后续别名自动归并；id=规范名 天然去重）
- ✅ 变更史每实体 ≤5 条（楼层+时间锚点）；状态 ≤80 字 / 变更 ≤60 字钳制
- ✅ 注入【实体台账】：用户消息/近期活跃打分取前 6 实体（当前态+最近 2 条变更史），预算 400 字；块头声明"权威数值以状态面板为准"

#### 3. 悬念簿（蓝图步骤 11）
- ✅ `[悬念]`（建立）/ `[悬念核销]`（收束）块协议；核销未建立的悬念按已核销存档（保留叙事事实）
- ✅ 重复建立不重复开线；注入【未决悬念】≤8 条 / 600 字，块头约束"核销前保持未解"

#### 4. 主观记忆批量提取（蓝图步骤 12）
- ✅ 挂纪事压缩成功后二次 LLM 调用（同一区间输入、走副API）：`{holder, kind:记得/相信/怀疑/误解, memory≤50字, known_by}`
- ✅ 幂等：rangeId（`l1_from_to`）去重，重试/重掷/重算安全；提取 await 于压缩互斥锁内（撤销回滚等待覆盖提取窗口，无竞态）
- ✅ 钳制：每人 ≤3 条 / 整批 ≤10 条；失败只告警不阻塞压缩链
- ✅ 注入【人物记忆】：主演/龙套分级（关系簿/任务发布者/本回合消息/近 30 行编年史命中=主演，每人 ≤3 条；龙套只注入最近 1 条），预算 500 字；块头标注"主观认知不代表世界事实"

#### 5. 链路整合
- ✅ response-parser 提取+剥离实体/悬念块（对齐任务块模式，支持 `[]`/`<>` 两种写法与繁体/英文别名）
- ✅ 归档链改写：`storeSummariesToMemory(summaries, {entities, suspenses})`；**无纪要但有账本变动时不推进楼层号**（避免编年史空洞），账本记录挂当前楼层
- ✅ 撤销链：快照含台账/悬念/主观记忆**全量内容**（比 id 差量精确——台账 upsert 原地更新可正确回滚）；旧快照无 P1 字段时跳过对应 store（向后兼容）
- ✅ 协议注入：「输出格式」新增「记忆账本块」章节（常驻）+「数据同步协议」边界条款（账本块不属于命令白名单）；转换器重跑（128 文件 / 0 严重问题 / 完整度 90/100 不变）
- ✅ 记忆库查看器 3→6 页签（常驻线/编年史/纪要/台账/悬念/人物记忆）；stats 行含 P1 计数；清空记忆覆盖新 store

#### 6. 回合终止功能（AI 思考时中断）

- ✅ 「⏹ 终止」按钮挂在加载指示器内（"AI 思考中..."旁），点击即中止本轮请求（AbortController → fetch signal）
- ✅ api-service：手动终止与内置超时区分（`isManualAbort` 标记，文案"已手动终止本轮生成"）；顺手修复 `_callGemini` fetch 漏传 signal 的 bug（此前 Gemini 无法中止）
- ✅ 语义：终止的本轮**不解析、不落地任何状态/记忆**；发送前快照留在撤销栈，可用「撤销上一回合」清掉已显示的输入行；`_gameBusy`/输入框在 finally 中恢复
- ✅ tools-test-abort.js 新增 6 项：OpenAI/Gemini 手动终止、超时区分、正常请求不受影响、调用级覆盖配置（副API路径）下同样生效

#### 测试与回归（2026-09-06）
- ✅ tools-test-story-engine.js 21→38 项：台账归并/变更史、悬念流转、提取幂等、主演/龙套分级、P1 注入块、P1 回滚、解析器提取/剥离、无纪要回合楼层语义、stats
- ✅ 回归：quest-block / narrative-render（34）/ status-commands / battle-math 全部通过；index.html 内联脚本语法 0 错误

---

### 2026-09-05 批：记忆系统 2.0 + 身份体系 + 体验批

#### 1. 记忆系统 2.0（解决「1000 层不混淆、每层可考、不依赖向量」）

- ✅ **五层架构**：逐字上下文（8 轮）→ 每层纪要（永久归档，`<SUMMARY>` 驱动）→ 编年史索引（每层一行，gameTime 时间锚点）→ 滚动合并常驻线（纪事 20 层 → 卷宗 → 典章，覆盖度驱动补压缩）→ 检索展开（词法关键词 + 用户片段兜底；向量可选）
- ✅ 新库 `calamity-memory` v1（summary/chronicle/story/embeddings 四 store）；旧 vecmem 库惰性留存
- ✅ 副 API 通道（`memory-api.js`）：记忆压缩走独立 url/key/model，未配置跟随主 API；api-service 支持调用级覆盖
- ✅ 撤销四层联动：快照含 storyIds/summaryFloors/chronicleFloors/vecIds；「吸收不删除」设计保证跨合并边界回滚无记忆真空；rollback 等待压缩互斥锁
- ✅ 旧 arc 压缩 / recent-ids 队列 / L2 事件层退役（分别由常驻线、台账(P1)替代）
- ✅ 记忆库查看器三页签（常驻线/编年史/纪要）

#### 2. 身份体系三档

- ✅ **开局包**：七身份差异化金币/主手/护甲/背包（`IDENTITY_KITS` 查表 + equipmentFactory 真实模板）；登记册即时预览；假"铁剑"移除
- ✅ **命运点联动**：新条目「身份体系」——身份时刻（契合身份且有实际剧情效果）结算 +1；频控（每日 1 次/场景去重/上限 3 不再发）；上限 1→3 + 旧存档迁移；每日刷新改"归零补 1 不回满"
- ✅ **身份体系脚本**（`identity-system.js`）：七身份注册表（领域检定优势 + 专业叙事视角 + 边界）；注册（saveCharacter）/读档（initGame）自动接入；prompt-builder 常驻注入；技能总纲优势三来源不叠加规则

#### 3. 体验与修复批

- ✅ 顶栏时间 + 角色信息面板位置（`progress.currentPlace` 两级显示；开局修复：initGame 统一走 refreshGameUI；旧存档自动迁移）
- ✅ 物品清单自动渲染卡片（段内连写/逐行列表两形态）；段落 `**加粗**` 正常渲染
- ✅ 正文剥离 HTML 注释（预设隐藏指令回显过滤）
- ✅ ST 预设管理面板（执行序列逐条启停/预览/筛选/批量；刷新后启用状态持久恢复）
- ✅ 采样参数接通：温度滑杆（去除硬编码 0.8）/ 上下文窗口 Token 预算裁剪（fitHistoryBudget）/ Top K 仅 Gemini
- ✅ `tools-serve.py` no-cache 开发服务器（ES 模块缓存坑解法）

---

## 📊 验证结果（2026-09-05）

- ✅ 任务块协议 10/10；叙事渲染 34/34（含物品卡片/委托卡新用例）；记忆系统 21/21（模拟 120 回合：合并触发/吸收标记/跨边界撤销/副API路由/向量门控）
- ✅ 浏览器真实 IDB 端到端：归档落盘 / 压缩触发 / 编年史渲染 / 词法命中展开 / 旧存档迁移（currentPlace + 命运点上限）
- ✅ 历史验证保留：32/32 运行时模块加载 / 内联脚本语法 0 错误 / 数据 key 一致性 / 端到端烟雾测试（2026-09-04）

---

## 🚀 下一步计划

1. **真实长跑验证（当前最有价值）**：连续游玩 20+ 层，观察记忆系统压缩质量、编年史命中率、身份时刻 AI 裁定的松紧、领域优势的体感，以及 P1 账本套件的块输出遵从率（[实体更新]/[悬念] 是否稳定输出、别名归并是否命中）
2. **身份第三档**（依赖实玩反馈）：七身份专属初始支线 + 声望跨档奖励（建议支线完成时 +15 跨入友善档）
3. **历史遗留**（见下「已知问题」）：equipment 双状态源收口（已消除）、法术总纲 sheet_spells 改命令流、NPC 六维补全
4. 可选小项：图形化地点面板（`unlockedLocations` 已在存档）；index.html 拆分；主观记忆 P2（hash 校验/摘要编辑）

---

## ⚠️ 已知问题

1. ~~**equipment 双状态源**~~：✅ 已消除（2026-09-05）——combat-system/creature-system 移入 `module/legacy/` 冻结；其生物模板已整合进世界书（10 只 JS 独有生物迁移为 yaml，5 只重叠核对一致）；同时修复 `buildCreatureWorldbook()` 在 Object.assign 时遮蔽静态 yaml 生物条目的活体 bug，生物数据单一事实源为 `世界书/生物/*.yaml`（32 只）
2. **opening-system.js 模块闲置**：开局叙事实际由内联 `IDENTITY_OPENINGS` + `calamityOpening` 通道实现（可用），模块本身未接线
3. **quest-system.js 旁路**：主链路收敛于 gameData.quests，独立 questManager 仍是并行代码
4. **身份时刻为 AI 裁定**：触发松紧依赖模型自觉，频控规则（每日 1 次/场景去重）无引擎硬约束——需实玩校准措辞
5. **P1 块协议为 AI 自觉输出**：[实体更新]/[悬念] 块漏输出时该回合世界事实/悬念不入账（无引擎强制）；台账只记 AI 认为值得记录的变化，不保证完备——需长跑观察遵从率
6. **主观记忆提取失败不重试**：区间纪事压缩成功但提取失败时，该区间记为"本轮未提取"（不进重试队列，等下一纪事区间）
7. **旧存档注意**：命运点上限 1→3 与 currentPlace 均有自动迁移；升级前的楼层无编年史行属预期；P1 三类账本从零累积
8. ~~**CORS 代理默认启用**~~：✅ 已消除（2026-09-05）——内置第三方公共代理地址已移除，代理默认关闭；需要中转时在设置页填入自建 Worker 地址启用；旧存档 localStorage 中残留的旧地址会在加载时自动清除

---

## 🔄 更新日志

### 2026-09-06（晚·机制×世界书对账）
- 升级公式重写（线性表/HP+10/PB 5-9-13-17/属性点）；属性点面板分配（B 方案 unspentPoints）；状态名对齐（出血/震慑/超重）；人情值钳制；测试 33 项+IAB 真机验证

### 2026-09-06（晚·桌面顶栏遮挡修复）
- 面板 grid-row:1/4→3/4（不再侵占顶栏行）；1920/390 双宽度断言+截图通过

### 2026-09-06（晚·自适应顶栏重构）
- 拆分顶栏信息+独立信息条；narrative副本 grid-column:1/3 覆盖级联致顶栏只跨中间列已修；窄屏 flex 流；IAB 390/700/1200 三宽度走查通过

### 2026-09-06（晚·手机适配）
- ≤900px：单列 + 左右滑出抽屉（角色/任务）+ 顶栏切换按钮；--app-height 键盘自适应 + 安全区 + 16px 输入；弹层全宽。IAB 390×844/1200×800 真机模拟验证通过，桌面零回归

### 2026-09-06（晚·叙事区持久化）
- narrative-display DOM 不持久化 → F5 后楼层消失但撤销按钮还在（localStorage/IDB 独立）。修复：displayNarrative 累加原文到 calamity-narrative-log（最多 200），initGame 优先按序重渲染，undoLastTurn 同步裁剪。tools-test-narrative-persist.js 8 项 exit=0

### 2026-09-06（晚·开局任务重复修复）
- applyQuests 同名去重（三态比对 + duplicated 报告）+ 开局数据块/输出格式双提示；quest-block 测试补 3 项 exit=0

### 2026-09-06（晚·负重机制落地）
- computeEncumbrance：装备+背包总重汇总（weight 字段 > 名称估算表 > 类型兜底），上限 10+力量×2
- 超重状态引擎自动落地/解除（conditions 白名单新成员，极端超重层级 2），AI/玩家双可见（状态块负重节 + 角色面板负重行）
- 背包系统/数据同步协议改为引擎结算口径；转换器重跑
- 测试：status-commands 扩至 27 项全过（修正 base() 缺字段致静默中断的测试缺陷）；全量回归 exit=0

### 2026-09-06（晚·命令引擎修复批）
- MP 写入通道：别名 MP/法力 → character.mp + 智力×5 钳制；角色.MP 大小写归一
- 装备槽中文别名归一（主手等 11 槽位）+ 旧存档中文键垃圾自动迁移清除
- 耐久语义化：add/set 装备.X.耐久 作用于 durability.current（此前战斗耐久从未真正扣过）
- 负重协议明令禁写（派生值不入档）；数据同步协议四处更新 + 转换器重跑
- tools-test-status-commands.js 8→21 项全过；全量回归通过

### 2026-09-06（记忆系统 P1·账本套件）
- 数据层：calamity-memory v1→v2 新增 ledger/suspense/subjective 三个 store（旧数据无损）
- 实体台账：[实体更新] 块协议 + 别名双向归并 + 变更史（≤5 条）+【实体台账】注入（≤6 实体/400 字）
- 悬念簿：[悬念]/[悬念核销] 块协议 + 重复建立幂等 +【未决悬念】注入（≤8 条/600 字，核销前保持未解）
- 主观记忆：挂纪事压缩批量提取（走副API、rangeId 幂等、锁内 await 无撤销竞态）+ 主演/龙套分级注入（≤500 字）
- 链路：解析器提取/剥离、归档链扩展（无纪要不推楼层）、撤销快照全量内容回滚（旧快照兼容）、查看器 6 页签
- 协议：输出格式新增「记忆账本块」章节（常驻注入）+ 数据同步协议边界条款；转换器重跑（完整度 90/100 不变）
- 测试：story-engine 测试 21→38 项全过；quest-block/narrative-render/status-commands/battle-math 回归通过

### 2026-09-05（晚·战斗引擎加固 P0/P1）
- P0 死引擎冻结：combat-system/creature-system 移入 `module/legacy/`，双状态源暗雷消除
- 生物数据整合：JS 独有 10 只迁移为世界书 yaml（世界书生物 22→32 只）；修复 `buildCreatureWorldbook` 在 Object.assign 时遮蔽静态 yaml 生物条目的活体 bug；生物单一事实源确立
- P1 战斗追踪器+侧栏面板：实现后经评估**移除**（玩家决定不需要战斗面板）——战斗过程仅保留在叙事区战斗卡内；敌人 HP 行协议随面板一并回退；蓝图 P2/P3（数学校验警示/长回合收敛）待定
- 附带清理：narrative-system 的死 import；index.html 两个冻结模块 import

### 2026-09-05
- 记忆系统 2.0：五层架构（纪要/编年史/滚动合并常驻线/词法+可选向量）+ 副API + 撤销联动；新库 calamity-memory；查看器三页签
- 身份体系三档：开局包 / 命运点联动（身份时刻·上限3）/ 领域优势+叙事视角脚本（identity-system.js）
- ST 预设管理面板 + 启用状态刷新恢复；采样参数全部接通（温度/上下文预算/Top K）
- UI 批：顶栏时间 + 位置两级显示 + 旧存档迁移；物品清单卡片；HTML 注释剥离；开局状态修复
- 工具：tools-serve.py（no-cache）、tools-test-story-engine.js（21 断言）；渲染测试扩至 34 项

### 2026-09-04（P0 修复批）
- 修复 index.html 死代码块（871 行）与两处死链；去除重复脚本加载
- 对话历史 / 任务块 / API 超时 / Gemini key 请求头四项主链路修复
- 转换器重写（扁平格式 + 包装标签剥离）；骰子池纳入数据并实时展开注入
- 协议一致性清理（废弃标签全改 `<命令>` 区）；命令引擎容器护栏；skills/spells 可写根
- worldbook 注入上限与优先级；rollDice 兼容导出；锻造/炼金检定契约修复
- 遗留模块移入 `module/legacy/`；variableSystem.getAll 补齐
- 任务简报面板 + 任务块协议 + 回合撤销 + 记忆库查看器（旧向量方案）

### 2026-09-03
- 初始化项目结构；116 YAML → 3 数据模块；核心系统实现；书式 SPA；AI 数据层接线

---

**下一步**：起本地服务（`python tools-serve.py 8090`）做 20+ 层真实长跑，喂养内容并校准身份时刻/记忆系统参数
