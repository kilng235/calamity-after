# 灾厄之后·重制版 更新日志

> 版本：2.1.0-alpha
> 更新时间：2026-09-06

---

## ✅ v2.1.0-alpha - 记忆系统 P1 账本套件（2026-09-06）

### 实体台账（蓝图步骤 10）

- **[实体更新] 块协议**：AI 在正文外输出实体块（名称/类别/别名/状态/变更，一块多实体空行分隔），解析器提取并从展示文本剥离
- **upsert + 别名双向归并**：规范名保持首次登记名，后续不同称呼自动归并进别名（防"同一实体多个名字"混淆）；变更史每实体 ≤5 条（楼层+时间锚点）
- **注入【实体台账】**：按"用户消息命中 + 近期活跃"打分取前 6 实体（当前态 + 最近 2 条变更史），预算 400 字；块头声明权威数值仍以状态面板为准

### 悬念簿（蓝图步骤 11）

- **[悬念] / [悬念核销] 块协议**：剧情抛出谜团时建立、收束时核销（写明结果）；重复建立不重复开线；核销未建立的悬念按已核销存档保留事实
- **注入【未决悬念】**：≤8 条 / 600 字，块头约束"核销前保持未解状态"，防悬念被 AI 无声遗忘或提前自我了结

### 主观记忆批量提取（蓝图步骤 12）

- **挂纪事压缩的二次 LLM 调用**（走副API）：`{holder, kind:记得/相信/怀疑/误解, memory≤50字, known_by知情者}`；每人 ≤3 条、整批 ≤10 条
- **幂等**：rangeId（l1_from_to）去重，重试/重掷/重算安全；提取 await 于压缩互斥锁内，撤销回滚等待覆盖提取窗口（无竞态）
- **主演/龙套分级注入**：【人物记忆】块 ≤500 字——关系簿/任务发布者/本回合消息/近 30 行编年史命中者为主演（每人 ≤3 条），龙套只注入最近 1 条；块头标注"主观认知不代表世界事实"（防 AI 全知）

### 桌面顶栏遮挡修复（面板 grid-row 越界）

- **现象**：桌面宽屏下左右面板从第 1 行（顶栏行）开始占位，面板背景盖住顶栏（面板 DOM 靠后且同为定位元素），只有 z-index:1 的玩家名/HP 浮在上面——视觉上顶栏被"吃掉"两端；手机端因 flex 流不受影响
- **修复**：`.left-panel`/`.right-panel` 的 `grid-row:1/4` → `grid-row:3/4`（只占叙事行，与原三栏设计一致）；验证 1920×1000 断言（顶栏满宽 y=0、面板 y=92 起、无重叠）+ 390 手机回归通过

### 自适应顶栏重构（手机重叠修复 + 多宽度自然重排）

- **根因**：旧版 ≤900px 媒体查询强制 grid-template-columns:1fr + grid-column:1/-1，但 narrative-render-style 块的 .top-bar{grid-column:1/3} 在级联后定义覆盖了主样式，导致顶栏只跨中间两列（1200px 下=920px），玩家名/HP/命运点/抽屉按钮与左面板叠合
- **拆分顶栏信息**：玩家名+核心资源（HP/命运点）+抽屉按钮 → 顶栏 .top-bar；次要信息（游戏内时间/金币）→ 新增 .info-bar（横排 32px 行，可滚动）
- **grid 修复**：所有面板显式 grid-column/grid-row（左1/2、右3/4、中心2/3、底栏1/-1、顶栏1/-1 跨全宽）；narrative-render-style 副本只补 color/font 等辅助属性，不重定义 grid
- **窄屏 flex 流**：≤900px 媒体查询彻底放弃 grid（grid 单列+多 auto-place 仍易分裂），改 display:flex column，每行 flex 高度显式声明，自然重排
- **信息条水平滚动**：.info-bar 设 overflow-x:auto + white-space:nowrap，时间/金币在窄屏可左右拖
- **验证（IAB 390×844 / 700×900 / 1200×800 三宽度浏览器真机截图）**：
  - 390：顶栏（玩家名+HP+命运点+抽屉按钮同行不重叠）+ 信息条 + 叙事 + 输入四段式
  - 700：自动收起为抽屉模式（left/right 滑出），信息条可见
  - 1200：三栏完整恢复+信息条+抽屉按钮隐藏，零回归
- index.html 语法 0 错误；全量 11 套件 exit=0

### 手机界面适配（≤900px 响应式抽屉布局）

- **布局**：≤900px 断点下三栏 grid 变单列（顶栏/叙事/输入），左右面板变 `position:fixed` 滑出抽屉（transform 动画），顶栏新增「📜 角色」「📋 任务」切换按钮（深底金字，对比度修正过一轮），互斥开合 + 遮罩点击关闭；>900px 三栏原样、按钮隐藏，桌面零回归
- **视口**：meta 增补 `viewport-fit=cover` + `interactive-widget=resizes-content`；`--app-height` 跟随 visualViewport（软键盘/工具栏收展自适应，输入区不被键盘遮挡）；`env(safe-area-inset-bottom)` 适配 iPhone 底部
- **输入与触摸**：输入框 16px（防 iOS 聚焦自动放大）、按钮/输入 min-height 44px 触摸目标、`-webkit-tap-highlight` 去闪、弹层（背包/设置/记忆查看器）小屏全宽
- **验证（浏览器真机模拟 IAB 390×844 / 1200×800）**：手机端单列+抽屉开合/互斥/遮罩关闭/角色面板完整（HP/MP/AC/负重）/输入字号全部断言通过并截图确认；桌面三栏与按钮隐藏断言通过；index.html 语法 0 错误；全量 10 测试套件 exit=0

### 叙事区持久化（修复 F5 刷新后楼层消失）

- **根因**：`narrative-display` DOM 不持久化，刷新后整页重渲染只剩欢迎语或开局叙事；撤销栈（localStorage 独立）与 IndexedDB（编年史/纪要）都在，所以撤销按钮能显示、记忆库能看到楼层——只缺正文 DOM
- **修复**：`displayNarrative` 每次累加原文到 `calamity-narrative-log`（localStorage，最多 200 条/≈100 层，超出从最旧裁）；`initGame` 在叙事区空时优先按日志按序重渲染（文档片段一次性插入，避免多次 layout），无日志时才显示欢迎语；`undoLastTurn` 同步按 `before-target` 移除尾部 N 条
- **测试**：新增 tools-test-narrative-persist.js 8 项（累加顺序/精确 trim/空安全/上限 200/损坏数据兜底/撤销联动），全量 8 套件 exit=0

### 开局任务重复登记修复

- **引擎同名去重**（applyQuests）：新任务块登记前按名称比对 进行中/已完成/失败 三态，已存在即拒绝入列并记入 `report.duplicated`——开局初始任务被 AI 第一回合重复上报 `[新任务]` 的直接根治
- **AI 侧提示**：开局数据块「初始任务」节加粗标注"已登记为进行中，禁止重复上报"；输出格式任务块节新增同名去重通用规则；转换器重跑
- 测试：quest-block 套件补 11~13（同名拒登/duplicated 报告/已完成态同样拒登），exit=0

### 负重机制落地（引擎自动结算）

- **computeEncumbrance**（command-processor）：装备 10 槽 + 背包（weight × 数量）汇总总重，上限 = 10 + 力量×2（背包系统规则）；供命令后校准、prompt-builder 状态块、角色面板三处共用
- **重量三级来源**：物品自带 weight 字段 > 名称关键词估算表（板甲15/锁甲10/皮甲5/盾4/双手剑4/铁剑2/匕首0.8/药剂0.3/矿石1.5 等，对齐装备总纲与背包系统重量表）> 类型兜底
- **超重状态自动落地/解除**：总重 > 上限 → conditions.超重 自动落地（白名单负面红标）；> 1.5×上限 → 层级 2（极端超重，行动受限）；卸载物品后自动解除；AI 用命令伪造/解除会被引擎以实际重量结算覆盖
- **AI 可见**：状态块新增「负重」节（当前总重/上限 + 超重/极端超重提示语），AI 每回合按实际值演出力量检定劣势等后果；新物品入包建议带 weight 字段
- **玩家可见**：角色信息面板新增负重行（超重标红）；超重层级标签（超重 ×2）
- 协议：背包系统「超重规则」改为引擎自动结算说明、数据同步协议派生值节更新；转换器重跑（完整度 90/100 不变）
- 测试：tools-test-status-commands.js 扩至 27 项（重量三级来源/装备背包计入/超重落地与解除/极端层级/命令伪造防护），并修正测试 base() 缺 equipment 字段导致 11 项起静默中断的问题（带退出码全量验证）

### 命令引擎修复批（登记档案暴露的缺口）

- **MP 写入通道补齐**：别名 `MP`/`法力`/`法力值` → `character.mp`（法术/炼金系统真实字段）；`角色.MP` 大小写自动归一；命令后校准按 智力×5 钳制上限（与 spell-system.calculateMPMax 同公式）——此前 MP 命令全部被拒或写成大写死数据
- **装备槽中文别名归一**：`装备.主手/副手/身体/头部/手部/腿部/脚部（足部）/肩部/饰品1/饰品2` → 标准槽位（mainHand 等）——此前落到中文键成静默死数据，面板永不渲染；旧存档中文键垃圾在每次命令结算时自动迁入标准槽位后清除（标准槽位已有装备时丢弃中文键版本）
- **耐久语义化**：`add/set 装备.主手.耐久 = N` 作用于装备对象 `durability.current`（按 max 钳到 0~max）——此前这条协议命令实际落在中文垃圾键，战斗从未真正扣过耐久；整对象 set 写法仍兼容
- **负重明确禁写**：协议（数据同步协议 §1）新增"无存储字段的派生值"节——负重按六维派生不入档；character.mp 字段行、装备槽别名、MP 别名行同步入协议文档，转换器重跑
- 测试：tools-test-status-commands.js 8→21 项（MP 通道/钳制、槽位归一、耐久语义化、旧存档垃圾键迁移、负重拒绝、全档案登记流程 0 拒绝）

### 回合终止（AI 思考时中断）

- 「⏹ 终止」按钮随"AI 思考中..."指示器出现，点击即中止本轮请求（AbortController → fetch signal，OpenAI/Gemini 均支持）
- 手动终止与内置超时区分（isManualAbort 标记）；修复 `_callGemini` 漏传 signal 的 bug
- 终止的本轮不解析、不落地任何状态与记忆；快照留在撤销栈，可撤销清除已显示的输入行
- tools-test-abort.js 新增 6 项断言（手动终止/超时区分/正常请求/覆盖配置路径）

### 链路与兼容

- 归档链扩展：摘要/实体/悬念同回合入库；**无纪要但有账本变动时不推进楼层号**（避免编年史空洞）
- 撤销链：快照含三类账本**全量内容**（台账 upsert 原地更新也能精确回滚）；P1 之前的旧快照自动跳过（向后兼容）
- 存储升级：calamity-memory v1→v2 新增三个 store，旧数据无损；查看器 3→6 页签；清空记忆覆盖新 store
- 协议注入：「输出格式」新增「记忆账本块」章节（常驻）+「数据同步协议」边界条款（账本块不属于命令白名单，防与命令引擎冲突）；转换器重跑（128 文件 / 完整度 90/100 不变）
- 测试：tools-test-story-engine.js 21→38 项全过；既有四套测试回归通过

### 封面视觉批：全屏卷宗 + 断剑模糊背景 + 高对比功能键（2026-09-06）

- **设置页快照残留修复**：移除 h1 与 api-feedback 上浏览器快照导出的内联几何（width:616px 等）——消除手机端标题偏右与文档横向溢出（scrollWidth 688→≤视口）；新增 `<meta name="color-scheme" content="only light">` + `:root{color-scheme:only light}`，阻止移动浏览器"强制深色"改写羊皮纸配色
- **封面全屏铺满**：新增 fullbleed-cover-style 补丁——body 去 20px 内边距，book-cover 去 720px 上限改 `min-height:var(--app-height,100vh)` + flex 拉伸羊皮纸；首页内容垂直居中；页脚说明（ALPHA 行）从封面外移入羊皮纸底部（深底浅字改纸面墨字）；登记册嵌套封面 `min-height:0` 防双重滚动；游戏视图不受影响（#view-game 为 position:fixed + ID 级 display 规则，flex 覆盖对其无效）
- **断剑插画模糊背景**：#view-home 新增 cover-bg 层（absolute inset:0 + overflow:hidden 裁剪），::before 超尺寸（inset:-16px）铺 `img/ui/cover-02-broken-sword.png`（center/cover）+ `blur(2px)`，层 opacity 0.4；防模糊边缘透明渗边；标题/按钮浮于其上（层级 0 < 烛光/边框 2 < 角饰 3 < 内容 5）
- **首页功能键高对比**：`#view-home .btn` 深皮底金字（配色对齐游戏内抽屉按钮），primary 金调皮革 + 金色光晕，hover 亮金边框 + 辉光，禁用态 opacity 0.4 保留；作用域限首页，设置/指南等页按钮维持原羊皮纸风格
- **验证（浏览器真实重载断言，390×844 / 477×862 / 1280×900）**：封面与视口 1:1 铺满、无横向溢出、路由显隐无泄漏、游戏视图 fixed 隔离（active 时仍 block+fixed 全屏）、背景层计算样式与图片加载、四键计算样式（深皮底/金字/禁用透明度）

---

## ✅ v2.0.0-alpha - 记忆系统 2.0 + 身份体系 + 体验批（2026-09-05）

### 记忆系统 2.0（重构）

- **五层记忆架构**：逐字上下文（8 轮）→ 每层纪要（永久归档）→ 编年史索引（每层一行，`第N层｜300年11月12日 09:30·铁矿镇｜摘要`）→ 滚动合并常驻线（纪事 20 层 → 卷宗 100 层 → 典章 300 层，"吸收不删除"保证撤销安全）→ 检索展开（词法为主、向量为可选增强）
- 常驻成本对数增长：1000 层 ≈ 6400 token / 2000 层 ≈ 8000
- **副 API 通道**：记忆压缩等 LLM 调用走独立配置（`calamity-memory-api`），未配置时跟随主 API
- **撤销四层联动**：回合撤销同步回滚 纪要/编年史/常驻线/向量，跨合并边界安全
- 存储迁移至新库 `calamity-memory`（summary/chronicle/story/embeddings 四 store）；旧 `calamity-vecmem` 惰性留存
- 记忆库查看器改三页签（常驻线/编年史/纪要）；设置页新增「记忆系统」节

### 身份体系（三档全落地）

- **开局包**：七身份差异化金币/武器/护甲/背包，全部使用 equipment-system 真实模板（伤害/AC/耐久真实生效）；登记册选身份即时预览；移除模板外的假"铁剑"
- **命运点联动**：新世界书条目「身份体系」——契合身份的「身份时刻」结算命运点 +1（每日 1 次/场景去重/上限 3）；命运点上限 1→3 并迁移旧存档；每日刷新改为归零补 1 不回满
- **身份体系脚本**（`identity-system.js`）：领域检定优势 + 专业叙事视角，注册/读档自动接入，prompt-builder 常驻注入；优势与种族/背景特质不叠加（技能总纲三来源规则）

### ST 预设

- 查看预设改为**管理面板**：按有效执行顺序逐条启用/禁用（即时保存生效）、内容预览、筛选与批量操作
- 页面刷新后自动恢复已启用预设的挂载（此前刷新即静默回退默认提示词）

### 界面与体验

- 顶栏时间 + 角色信息面板位置（`据点 · 具体场所`，新字段 `progress.currentPlace`，AI 经命令更新，旧存档自动迁移）
- 叙事区物品清单自动渲染为卡片（段内连写与逐行列表通吃），普通段落 `**加粗**` 正常渲染去星号
- 正文剥离 HTML 注释（预设隐藏指令被 AI 回显时不再显示）
- 开局时间/地点不更新修复（initGame 统一走 refreshGameUI）

### 采样参数（全部接通）

- 温度滑杆生效于主剧情（此前被硬编码 0.8 覆盖）
- 「上下文窗口 Token」实现真实历史预算裁剪（超预算从最旧丢起，系统提示词与最新消息保留）
- Top K 仅对 Gemini 发送（OpenAI 兼容接口忽略该字段）

### 工具

- `tools-serve.py`：no-cache 开发服务器（ES 模块缓存导致更新不生效的解法）
- `tools-test-story-engine.js`：记忆系统 21 项断言；渲染测试扩至 34 项

---

## ✅ v1.2.0-alpha - P0 修复批 + 任务简报/撤销（2026-09-04）

- P0 修复批 29 项提交：死代码清理（871 行）/ 对话历史滚动窗口 / 任务块链路收敛 / API 超时 / Gemini key 请求头 / 转换器重写 / 协议一致性 / 命令引擎护栏 / worldbook 注入上限 / 遗留模块冻结（详见 PROJECT-STATUS 2026-09-04）
- 任务简报面板（右栏）+ 任务块协议（`[新任务]/[任务完成]/[任务失败]`）+ 叙事任务卡片
- 回合撤销功能（静默回滚：变量 + 对话历史 + 叙事页 + 向量记忆）
- 登记册第Ⅲ页「启程」移除并入游戏叙事区；设置页返回上一页；快捷操作加返回首页
- 旧向量记忆接线（已在 v2.0.0 重构为记忆系统 2.0）

---

## 📜 装备系统专项日志（v1.1.0，2026-09-02）

# 装备系统更新日志

> 版本：1.1.0  
> 更新时间：2026-09-02 21:30

---

## ✅ v1.1.0 - 双持系统

### 新增功能

#### 1. 双持战斗系统
```javascript
// 双持检测
isDualWielding() → boolean

// 双持信息
getDualWieldInfo() → { mainHand, offHand, canAttackWithBoth }

// 主手攻击（正常伤害）
rollWeaponDamage(attributes, false)
// 伤害 = 骰值 + 材料加成 + 力量调整值

// 副手攻击（不加属性）
rollWeaponDamage(attributes, true)
// 伤害 = 骰值 + 材料加成 + 0（DND 5E 规则）
```

#### 2. 装备限制检查
```
✅ 主手必须有单手武器
✅ 主手不能是双手武器
✅ 副手必须是单手武器
✅ 副手不能是远程武器
✅ 装备副手时自动检查双持条件
```

#### 3. 耐久系统更新
```javascript
// 支持主手/副手耐久损耗
damageWeaponDurability(criticalFailure, useOffHand)
```

---

## 📊 双持机制

### DND 5E 规则
```
1. 主手攻击（动作）
   - 伤害骰 + 属性调整值

2. 副手攻击（附赠动作）
   - 伤害骰 + 0（不加属性）
   
3. 武器限制
   - 双方都是单手近战武器
   - 副手武器必须是"轻型"（简化：允许所有单手）
```

### 灾厄实现
```
✅ 完全符合 DND 5E 核心规则
✅ 简化了"轻型"武器限制
✅ 副手攻击不加属性调整值
✅ 双持时无命中惩罚
```

---

## 🎮 使用示例

### 装备双持
```javascript
// 1. 创建主手武器
const mainSword = createWeapon('废土短刃', 1);
equipItem(mainSword);

// 2. 创建副手武器
const offDagger = createWeapon('废土匕首', 1);
equipItem(offDagger);

// 3. 检查双持状态
if (isDualWielding()) {
  console.log('✓ 双持激活');
}
```

### 双持攻击
```javascript
// 主手攻击
const mainDamage = rollWeaponDamage(attributes, false);
// 例：1d6 + 0 + 2 = 3-8 伤害

// 副手攻击
const offDamage = rollWeaponDamage(attributes, true);
// 例：1d4 + 0 + 0 = 1-4 伤害

// 总伤害
const total = mainDamage.damage + offDamage.damage;
```

---

## 📈 双持优劣势

### 优势
```
✅ 攻击次数 ×2
✅ 总伤害输出更高
✅ 命中失败时有备用攻击
```

### 劣势
```
❌ 副手攻击不加属性
❌ 无法装备盾牌（失去 +2 AC）
❌ 两把武器都会损耗耐久
```

---

## 🔄 v1.0.0 → v1.1.0 变更

### 新增 API
```javascript
// 新增函数
+ isDualWielding()
+ getDualWieldInfo()
+ checkDualWieldRequirements()

// 修改函数
~ rollWeaponDamage(attributes, useOffHand)
~ damageWeaponDurability(criticalFailure, useOffHand)
~ equipItem() - 增加双持检查
```

### 兼容性
```
✅ 向后兼容
✅ 旧代码无需修改
✅ rollWeaponDamage() 默认主手攻击
```

---

## 🧪 测试功能

### 新增测试按钮
```
🗡️ 主手攻击 - 测试主手伤害
🔪 副手攻击 - 测试副手伤害（不加属性）
⚔️ 双持攻击 - 测试主+副总伤害
```

### 双持状态显示
```
⚔️ 双持模式激活
主手：废土短刃
副手：废土匕首
ℹ️ 主手攻击加属性调整，副手攻击不加属性调整
```

---

## 📋 未来扩展

### 可选功能（以后）
```
1. 轻型武器属性
   - 只有轻型武器才能双持
   
2. 双武器战斗专长
   - 副手攻击也加属性调整值
   
3. 双持技能
   - 旋风斩：同时攻击两个目标
   - 连击：两次都命中时额外伤害
```

---

## ✅ 完成清单

- [x] 双持装备检查
- [x] 双持攻击（主手/副手）
- [x] 副手攻击不加属性
- [x] 双持状态检测
- [x] 双手武器冲突检测
- [x] 远程武器限制
- [x] 双持耐久损耗
- [x] 测试页面更新
- [x] 设计文档

---

**双持系统完成！完全符合 DND 5E 规则，简化了部分限制以提升游戏体验。** ⚔️
