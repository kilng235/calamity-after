# 灾厄之后·重制版 - 项目摘要

> **更新时间**：2026-09-04
> **项目阶段**：P0 修复完成（结构/协议/链路），待真实 LLM 端到端测试
> **状态**：✅ 32 个运行时模块全部可加载；数据工作流已修复（`npm run convert` 一键再生成）

---

## 📦 项目位置

```
📂 灾厄之后-独立版\
├── 📄 index.html                     唯一入口（书式 SPA：首页/登记册/游戏/设置/指南）
├── 📁 module/                        32 个运行时模块（游戏逻辑 ES Modules + AI 层 IIFE）
├── 📁 module/legacy/                 8 个冻结的姬侠传遗留模块（不参与运行时）
├── 📁 data-source/                   117 个 YAML/TXT 世界书 + 转换器
├── 📁 docs/                          设计文档与报告
├── 📁 img/ui/                        UI 图片资源
├── 📄 测试-*.html                    19 个测试页（本地调试，不入库）
└── 📄 README.md / PROJECT-STATUS.md / SUMMARY.md / CHANGELOG.md
```

---

## ✅ 2026-09-04 修复摘要

| 类别 | 修复内容 |
|------|----------|
| **结构** | 删除 index.html 871 行死代码块（语法错误的旧脚本）；死链改 SPA 路由；去重复脚本加载 |
| **主链路** | 多轮对话历史（滚动 8 轮）；任务块 → applyQuests 落地；API 180s 超时中断；Gemini key 走请求头 |
| **数据层** | 转换器重写（扁平 promptData 格式 + 包装标签剥离）；骰子池纳入数据；117 条全量再生成 |
| **协议** | 废弃值标签（`<relation>/<gold>/<skill>/<exp>/<spell>`）全部改 `<命令>` 区；`<user>` 宏清理 |
| **骰子池** | `{{roll:dN}}` 宏每轮实时展开注入（反作弊设计在独立链路生效） |
| **命令引擎** | 容器整体赋值护栏；新增 `技能列表/法术列表` 可写根 |
| **制作系统** | rollDice 兼容导出修复（词缀/锻造/炼金恢复可加载）；检定契约对齐 nat20/nat1 字段 |
| **注入治理** | worldbook 上限 8 条 / 6000 字符，NPC > 检定 > 世界条目优先级 |
| **卫生** | 7 个冻结模块移入 legacy/；variableSystem.getAll 补齐；typo 修复 |

**验证**：32/32 模块加载 ✓；内联脚本 0 语法错误 ✓；key 一致性（116+1）✓；端到端烟雾测试全绿 ✓

---

## ⏳ 待完成

1. **真实 LLM 端到端测试**：配置 API Key 验证完整链路（核心）
2. **制作系统接入运行时**：锻造/炼金/词缀/法术/开局 5 系统接入 index.html；测试页改 import 真模块
3. **equipment 状态源收口**；opening-system 接线；法术总纲 sheet_spells 重写
4. **NPC 数据补充**：6 个 NPC 六维属性（90 → 97 分）

---

## 🚀 下一步行动

1. 配置 API Key（设置页），做真实 LLM 端到端测试
2. 按 PROJECT-STATUS「下一步计划」推进制作系统接入

---

**启动方式**：`cd 灾厄之后-独立版 && python -m http.server 8080`
**数据再生成**：`npm run convert`
